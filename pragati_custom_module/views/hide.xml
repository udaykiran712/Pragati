<odoo>
    <data>
        <record id="view_picking_form_inherit" model="ir.ui.view">
            <field name="name">stock.picking.form.inherit</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">
                <!-- Add the field inside the form -->
                <xpath expr="//form" position="inside">
                    <field name="hide_validate_button" invisible="1"/>
                </xpath>

                <!-- Hide the Validate button for Acharyulu -->
                <xpath expr="//header/button[contains(@name, 'button_validate')]" position="attributes">
                    <attribute name="attrs">{'invisible': [('hide_validate_button', '=', True)]}</attribute>
                </xpath>

            </field>
        </record>
    </data>
</odoo>


